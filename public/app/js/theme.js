  const THEME_PRESETS = {
    dark: {
      label: "Midnight",
      description: "Deep indigo-navy surfaces with bright lettering for TV screens, dim rooms, and glare-heavy walls.",
      previewBg: "#0a1330",
      previewPanel: "rgba(33, 48, 107, .96)",
      previewText: "#f6f9ff",
      previewMuted: "#c6cdf0",
      previewAccent: "#8ab4ff",
      bg: "#0a1330",
      text: "#f6f9ff",
      muted: "#c6cdf0",
      border: "rgba(200,210,255,.16)",
      panel: "rgba(33, 48, 107, .94)",
      panel2: "rgba(12, 20, 52, .82)",
      headerBg: "rgba(8,14,36,.85)",
      drawerOverlay: "rgba(0,0,0,.58)",
      btnBg: "rgba(255,255,255,.10)",
      btnBgHover: "rgba(255,255,255,.16)",
      inputBg: "rgba(255,255,255,.08)",
      inputPlaceholder: "rgba(233,238,252,.66)",
      focusRing: "rgba(138,180,255,.52)",
      toastBg: "rgba(20, 30, 70, .98)",
      danger: "#ff7a7a",
      displayFontColor: "#f6f9ff",
      displayMutedColor: "#c6cdf0",
      shadowColor: "rgba(0,0,0,.48)",
      cardTextMode: "auto",
    },
    slate: {
      label: "Graphite",
      description: "Neutral gunmetal charcoal with crisp white type and silvery, color-free accents.",
      previewBg: "#17191d",
      previewPanel: "rgba(43, 47, 54, .96)",
      previewText: "#f3f5f8",
      previewMuted: "#c2c7d0",
      previewAccent: "#9aa6b8",
      bg: "#17191d",
      text: "#f3f5f8",
      muted: "#c2c7d0",
      border: "rgba(200,205,215,.15)",
      panel: "rgba(43, 47, 54, .95)",
      panel2: "rgba(26, 29, 34, .86)",
      headerBg: "rgba(20,22,26,.86)",
      drawerOverlay: "rgba(0,0,0,.54)",
      btnBg: "rgba(255,255,255,.09)",
      btnBgHover: "rgba(255,255,255,.15)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(233,238,252,.62)",
      focusRing: "rgba(154,166,184,.46)",
      toastBg: "rgba(43, 47, 54, .98)",
      danger: "#ff7b78",
      displayFontColor: "#f3f5f8",
      displayMutedColor: "#c2c7d0",
      shadowColor: "rgba(0,0,0,.44)",
      cardTextMode: "auto",
    },
    cobalt: {
      label: "Cobalt",
      description: "Saturated electric royal blue with vivid blue surfaces and bright white text.",
      previewBg: "#04173f",
      previewPanel: "rgba(16, 62, 156, .96)",
      previewText: "#f5fbff",
      previewMuted: "#c2d8f6",
      previewAccent: "#4d8bff",
      bg: "#04173f",
      text: "#f5fbff",
      muted: "#c2d8f6",
      border: "rgba(180,210,255,.20)",
      panel: "rgba(16, 62, 156, .95)",
      panel2: "rgba(6, 28, 72, .85)",
      headerBg: "rgba(5,22,55,.87)",
      drawerOverlay: "rgba(0,0,0,.56)",
      btnBg: "rgba(255,255,255,.10)",
      btnBgHover: "rgba(255,255,255,.16)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(229,241,255,.66)",
      focusRing: "rgba(77,139,255,.50)",
      toastBg: "rgba(16, 62, 156, .98)",
      danger: "#ff7b78",
      displayFontColor: "#f5fbff",
      displayMutedColor: "#c2d8f6",
      shadowColor: "rgba(0,0,0,.46)",
      cardTextMode: "auto",
    },
    forest: {
      label: "Evergreen",
      description: "Deep emerald pine surfaces with bright neutral text for long, readable sessions.",
      previewBg: "#071d13",
      previewPanel: "rgba(16, 64, 42, .96)",
      previewText: "#f5fff9",
      previewMuted: "#c4e2d2",
      previewAccent: "#34d98b",
      bg: "#071d13",
      text: "#f5fff9",
      muted: "#c4e2d2",
      border: "rgba(190,226,210,.18)",
      panel: "rgba(16, 64, 42, .95)",
      panel2: "rgba(8, 34, 22, .85)",
      headerBg: "rgba(6,26,17,.87)",
      drawerOverlay: "rgba(0,0,0,.56)",
      btnBg: "rgba(255,255,255,.09)",
      btnBgHover: "rgba(255,255,255,.15)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(229,245,236,.62)",
      focusRing: "rgba(52,217,139,.38)",
      toastBg: "rgba(16, 64, 42, .98)",
      danger: "#ff7b78",
      displayFontColor: "#f5fff9",
      displayMutedColor: "#c4e2d2",
      shadowColor: "rgba(0,0,0,.44)",
      cardTextMode: "auto",
    },
    ocean: {
      label: "Oceanic",
      description: "Deep teal-cyan dark mode with crisp white text and a bright aqua accent.",
      previewBg: "#04181f",
      previewPanel: "rgba(12, 70, 88, .96)",
      previewText: "#f4fcff",
      previewMuted: "#bfe0e9",
      previewAccent: "#27d3ee",
      bg: "#04181f",
      text: "#f4fcff",
      muted: "#bfe0e9",
      border: "rgba(190,225,235,.18)",
      panel: "rgba(12, 70, 88, .95)",
      panel2: "rgba(6, 30, 38, .85)",
      headerBg: "rgba(5,22,28,.87)",
      drawerOverlay: "rgba(0,0,0,.56)",
      btnBg: "rgba(255,255,255,.09)",
      btnBgHover: "rgba(255,255,255,.15)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(226,244,250,.62)",
      focusRing: "rgba(39,211,238,.36)",
      toastBg: "rgba(12, 70, 88, .98)",
      danger: "#ff7b78",
      displayFontColor: "#f4fcff",
      displayMutedColor: "#bfe0e9",
      shadowColor: "rgba(0,0,0,.44)",
      cardTextMode: "auto",
    },
    ember: {
      label: "Ember",
      description: "Warm copper-and-rust surfaces with creamy text and a glowing orange accent.",
      previewBg: "#1f0e09",
      previewPanel: "rgba(90, 38, 20, .96)",
      previewText: "#fff8f3",
      previewMuted: "#ecd2c2",
      previewAccent: "#ff7a33",
      bg: "#1f0e09",
      text: "#fff8f3",
      muted: "#ecd2c2",
      border: "rgba(236,210,194,.18)",
      panel: "rgba(90, 38, 20, .95)",
      panel2: "rgba(34, 16, 10, .86)",
      headerBg: "rgba(30,14,9,.88)",
      drawerOverlay: "rgba(0,0,0,.56)",
      btnBg: "rgba(255,255,255,.08)",
      btnBgHover: "rgba(255,255,255,.14)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(255,239,228,.62)",
      focusRing: "rgba(255,122,51,.36)",
      toastBg: "rgba(90, 38, 20, .98)",
      danger: "#ff8b7a",
      displayFontColor: "#fff8f3",
      displayMutedColor: "#ecd2c2",
      shadowColor: "rgba(0,0,0,.44)",
      cardTextMode: "auto",
    },
    plum: {
      label: "Plum",
      description: "Rich aubergine-purple surfaces with creamy text and a bright orchid accent.",
      previewBg: "#170d24",
      previewPanel: "rgba(58, 31, 87, .96)",
      previewText: "#faf5ff",
      previewMuted: "#d8c7ea",
      previewAccent: "#c084fc",
      bg: "#170d24",
      text: "#faf5ff",
      muted: "#d8c7ea",
      border: "rgba(222,205,240,.18)",
      panel: "rgba(58, 31, 87, .95)",
      panel2: "rgba(28, 15, 42, .85)",
      headerBg: "rgba(22,12,34,.87)",
      drawerOverlay: "rgba(0,0,0,.56)",
      btnBg: "rgba(255,255,255,.09)",
      btnBgHover: "rgba(255,255,255,.15)",
      inputBg: "rgba(255,255,255,.07)",
      inputPlaceholder: "rgba(244,235,255,.62)",
      focusRing: "rgba(192,132,252,.40)",
      toastBg: "rgba(58, 31, 87, .98)",
      danger: "#ff8b9a",
      displayFontColor: "#faf5ff",
      displayMutedColor: "#d8c7ea",
      shadowColor: "rgba(0,0,0,.46)",
      cardTextMode: "auto",
    },
    light: {
      label: "Daylight",
      description: "Cool blue-white workspace with charcoal text and a crisp blue accent.",
      previewBg: "#eef3fb",
      previewPanel: "#ffffff",
      previewText: "#111827",
      previewMuted: "#334155",
      previewAccent: "#2563eb",
      bg: "#eef3fb",
      text: "#111827",
      muted: "#334155",
      border: "rgba(15,23,42,.18)",
      panel: "rgba(255,255,255,.96)",
      panel2: "rgba(232,239,250,.96)",
      headerBg: "rgba(244,248,253,.92)",
      drawerOverlay: "rgba(15,23,42,.24)",
      btnBg: "rgba(15,23,42,.07)",
      btnBgHover: "rgba(15,23,42,.13)",
      inputBg: "rgba(255,255,255,.96)",
      inputPlaceholder: "rgba(15,23,42,.46)",
      focusRing: "rgba(37,99,235,.38)",
      toastBg: "rgba(255,255,255,.98)",
      danger: "#b42318",
      displayFontColor: "#111827",
      displayMutedColor: "#334155",
      shadowColor: "rgba(15,23,42,.18)",
      cardTextMode: "auto",
    },
    ivory: {
      label: "Ivory",
      description: "Warm cream paper tones with dark ink text and a soft gold accent.",
      previewBg: "#f6efdd",
      previewPanel: "#fffaf0",
      previewText: "#1f2937",
      previewMuted: "#475569",
      previewAccent: "#c0851f",
      bg: "#f6efdd",
      text: "#1f2937",
      muted: "#475569",
      border: "rgba(120,90,20,.18)",
      panel: "rgba(255,250,240,.96)",
      panel2: "rgba(245,237,221,.94)",
      headerBg: "rgba(247,240,225,.92)",
      drawerOverlay: "rgba(55,65,81,.24)",
      btnBg: "rgba(55,65,81,.08)",
      btnBgHover: "rgba(55,65,81,.14)",
      inputBg: "rgba(255,255,255,.94)",
      inputPlaceholder: "rgba(31,41,55,.42)",
      focusRing: "rgba(180,83,9,.28)",
      toastBg: "rgba(255,250,242,.98)",
      danger: "#b42318",
      displayFontColor: "#1f2937",
      displayMutedColor: "#475569",
      shadowColor: "rgba(55,65,81,.16)",
      cardTextMode: "auto",
    },
    arctic: {
      label: "Arctic",
      description: "Icy cool surfaces with dark ink text and a bright cyan accent for sharp visibility.",
      previewBg: "#e7f3ff",
      previewPanel: "#ffffff",
      previewText: "#0f172a",
      previewMuted: "#334155",
      previewAccent: "#0ea5e9",
      bg: "#e7f3ff",
      text: "#0f172a",
      muted: "#334155",
      border: "rgba(14,116,180,.15)",
      panel: "rgba(255,255,255,.97)",
      panel2: "rgba(225,240,255,.96)",
      headerBg: "rgba(240,248,255,.94)",
      drawerOverlay: "rgba(15,23,42,.24)",
      btnBg: "rgba(37,99,235,.06)",
      btnBgHover: "rgba(37,99,235,.11)",
      inputBg: "rgba(255,255,255,.96)",
      inputPlaceholder: "rgba(15,23,42,.44)",
      focusRing: "rgba(14,165,233,.28)",
      toastBg: "rgba(255,255,255,.98)",
      danger: "#b42318",
      displayFontColor: "#0f172a",
      displayMutedColor: "#334155",
      shadowColor: "rgba(15,23,42,.16)",
      cardTextMode: "auto",
    },
    sand: {
      label: "Sandstone",
      description: "Warm terracotta-clay surfaces with dark slate text and a burnt-orange accent.",
      previewBg: "#f3e6d2",
      previewPanel: "#fffaf0",
      previewText: "#1f2937",
      previewMuted: "#4b5563",
      previewAccent: "#d96b0a",
      bg: "#f3e6d2",
      text: "#1f2937",
      muted: "#4b5563",
      border: "rgba(150,80,20,.16)",
      panel: "rgba(255,250,240,.96)",
      panel2: "rgba(244,233,212,.95)",
      headerBg: "rgba(246,236,219,.93)",
      drawerOverlay: "rgba(55,65,81,.22)",
      btnBg: "rgba(120,53,15,.07)",
      btnBgHover: "rgba(120,53,15,.12)",
      inputBg: "rgba(255,255,255,.94)",
      inputPlaceholder: "rgba(31,41,55,.42)",
      focusRing: "rgba(217,119,6,.28)",
      toastBg: "rgba(255,250,241,.98)",
      danger: "#b42318",
      displayFontColor: "#1f2937",
      displayMutedColor: "#4b5563",
      shadowColor: "rgba(55,65,81,.16)",
      cardTextMode: "auto",
    },
    sage: {
      label: "Sage",
      description: "Herbal green-tinted light surfaces with dark text and an emerald accent.",
      previewBg: "#e6f1e7",
      previewPanel: "#fbfefb",
      previewText: "#1f2937",
      previewMuted: "#475569",
      previewAccent: "#2f9e6f",
      bg: "#e6f1e7",
      text: "#1f2937",
      muted: "#475569",
      border: "rgba(20,110,70,.15)",
      panel: "rgba(251,254,251,.96)",
      panel2: "rgba(232,243,234,.95)",
      headerBg: "rgba(238,246,239,.93)",
      drawerOverlay: "rgba(55,65,81,.22)",
      btnBg: "rgba(5,150,105,.06)",
      btnBgHover: "rgba(5,150,105,.11)",
      inputBg: "rgba(255,255,255,.95)",
      inputPlaceholder: "rgba(31,41,55,.42)",
      focusRing: "rgba(47,158,111,.26)",
      toastBg: "rgba(251,254,251,.98)",
      danger: "#b42318",
      displayFontColor: "#1f2937",
      displayMutedColor: "#475569",
      shadowColor: "rgba(55,65,81,.16)",
      cardTextMode: "auto",
    },
    rose: {
      label: "Rose",
      description: "Soft blush-pink light surfaces with dark text and a vivid raspberry accent.",
      previewBg: "#fdeef2",
      previewPanel: "#fffafb",
      previewText: "#1f2937",
      previewMuted: "#4b5563",
      previewAccent: "#e11d72",
      bg: "#fdeef2",
      text: "#1f2937",
      muted: "#4b5563",
      border: "rgba(160,30,80,.14)",
      panel: "rgba(255,250,251,.96)",
      panel2: "rgba(252,232,238,.95)",
      headerBg: "rgba(253,238,242,.93)",
      drawerOverlay: "rgba(55,65,81,.22)",
      btnBg: "rgba(225,29,114,.06)",
      btnBgHover: "rgba(225,29,114,.11)",
      inputBg: "rgba(255,255,255,.95)",
      inputPlaceholder: "rgba(31,41,55,.42)",
      focusRing: "rgba(225,29,114,.26)",
      toastBg: "rgba(255,250,251,.98)",
      danger: "#b42318",
      displayFontColor: "#1f2937",
      displayMutedColor: "#4b5563",
      shadowColor: "rgba(55,65,81,.16)",
      cardTextMode: "auto",
    }
  };

  const THEME_DEFAULT_PRESET = "dark";

  function getThemePresetNames(){
    return Object.keys(THEME_PRESETS);
  }

  function isThemePresetName(name){
    return !!(name && THEME_PRESETS[name]);
  }

  const THEME_PREFS_STORAGE_PREFIX = "roomboard.website.themePrefs.v1";
  let activeThemePrefsScope = "guest";
  let themePrefsState = null;

  function getThemeScopeFromSession(sessionLike){
    const practiceId = window.__roomboardPracticeId || null;
    if(practiceId) return practiceId;
    return "guest";
  }

  function normalizeThemePrefs(raw){
    const prefs = raw && typeof raw === "object" ? raw : {};
    const current = isThemePresetName(prefs.themePreset)
      ? prefs.themePreset
      : (isThemePresetName(prefs.themeDefaultPreset) ? prefs.themeDefaultPreset : THEME_DEFAULT_PRESET);
    const defp = isThemePresetName(prefs.themeDefaultPreset) ? prefs.themeDefaultPreset : current;
    const theme = THEME_PRESETS[current] || THEME_PRESETS[THEME_DEFAULT_PRESET];
    return {
      themePreset: current,
      themeDefaultPreset: defp,
      bgColor: prefs.bgColor || theme.bg
    };
  }

  function getThemeStorageKey(scope){
    return THEME_PREFS_STORAGE_PREFIX + "." + (scope || "guest");
  }

  function readStoredThemePrefs(scope){
    try{
      const raw = localStorage.getItem(getThemeStorageKey(scope));
      if(!raw) return null;
      return normalizeThemePrefs(JSON.parse(raw));
    }catch(e){
      return null;
    }
  }

  function writeStoredThemePrefs(scope, prefs){
    try{
      localStorage.setItem(getThemeStorageKey(scope), JSON.stringify(normalizeThemePrefs(prefs)));
    }catch(e){}
  }

  function ensureThemePrefsLoaded(scope){
    const nextScope = scope || activeThemePrefsScope || "guest";
    if(themePrefsState && activeThemePrefsScope === nextScope) return themePrefsState;

    let prefs = readStoredThemePrefs(nextScope);
    if(prefs) writeStoredThemePrefs(nextScope, prefs);

    activeThemePrefsScope = nextScope;
    themePrefsState = normalizeThemePrefs(prefs);
    return themePrefsState;
  }

  // The active theme is per-window/tab. It lives in sessionStorage (which is
  // scoped to a single tab and survives reloads, but never crosses tabs or
  // syncs to the account), so changing the look in one window leaves every
  // other window — and the saved default — untouched.
  const WINDOW_THEME_STORAGE_PREFIX = "roomboard.website.windowTheme.v1";

  function getWindowThemeStorageKey(scope){
    return WINDOW_THEME_STORAGE_PREFIX + "." + (scope || "guest");
  }

  function readWindowThemeOverride(scope){
    try{
      const raw = sessionStorage.getItem(getWindowThemeStorageKey(scope));
      return isThemePresetName(raw) ? raw : null;
    }catch(e){
      return null;
    }
  }

  function writeWindowThemeOverride(scope, name){
    try{
      if(isThemePresetName(name)) sessionStorage.setItem(getWindowThemeStorageKey(scope), name);
      else sessionStorage.removeItem(getWindowThemeStorageKey(scope));
    }catch(e){}
  }

  // What this window should actually render: its own override if one was
  // chosen here, otherwise the account-wide default (what every fresh login
  // and new window starts on).
  function getActiveThemeName(){
    const override = readWindowThemeOverride(activeThemePrefsScope);
    if(override) return override;
    return getSavedThemeDefaultPreset();
  }

  function setWindowThemePreset(preset){
    const p = isThemePresetName(preset) ? preset : getSavedThemeDefaultPreset();
    writeWindowThemeOverride(activeThemePrefsScope, p);
    return p;
  }

  function clearWindowThemePreset(){
    writeWindowThemeOverride(activeThemePrefsScope, null);
  }

  function persistThemePrefs(){
    if(!themePrefsState) themePrefsState = normalizeThemePrefs(null);
    writeStoredThemePrefs(activeThemePrefsScope, themePrefsState);
    if(state && state.settings){
      state.settings.themePreset = themePrefsState.themePreset;
      state.settings.themeDefaultPreset = themePrefsState.themeDefaultPreset;
      state.settings.bgColor = themePrefsState.bgColor;
      saveLocal();
      if(!supabase || !currentPracticeId) noteSettingsLocalSaved("Saved locally");
      else noteSettingsRemoteQueued("Saving changes…");
      scheduleRemoteSave("userSettings", { immediate: true });
    }
  }

  function getThemeSettings(){
    return ensureThemePrefsLoaded(activeThemePrefsScope);
  }

  function applyThemePreset(presetName){
    const name = isThemePresetName(presetName) ? presetName : THEME_DEFAULT_PRESET;
    const t = THEME_PRESETS[name];
    const root = document.documentElement;

    root.style.setProperty("--bg", t.bg);
    root.style.setProperty("--bgSolid", t.bg);
    root.style.setProperty("--text", t.text);
    root.style.setProperty("--muted", t.muted);

    root.style.setProperty("--border", t.border);
    root.style.setProperty("--headerBorder", t.border);
    root.style.setProperty("--panel", t.panel);
    root.style.setProperty("--panel2", t.panel2);
    root.style.setProperty("--cardBg", t.panel);
    root.style.setProperty("--cardBorder", t.border);
    root.style.setProperty("--drawerBg", t.panel);

    root.style.setProperty("--headerBg", t.headerBg);
    root.style.setProperty("--drawerOverlay", t.drawerOverlay);

    root.style.setProperty("--btnBg", t.btnBg);
    root.style.setProperty("--btnBgHover", t.btnBgHover);
    root.style.setProperty("--btnBorder", t.border);
    root.style.setProperty("--btnText", t.text);
    root.style.setProperty("--inputBg", t.inputBg);
    root.style.setProperty("--inputBorder", t.border);
    root.style.setProperty("--inputText", t.text);
    root.style.setProperty("--inputPlaceholder", t.inputPlaceholder);

    root.style.setProperty("--focusRing", t.focusRing);
    root.style.setProperty("--toastBg", t.toastBg);
    root.style.setProperty("--toastBorder", t.border);
    root.style.setProperty("--toastText", t.text);
    root.style.setProperty("--danger", t.danger);
    root.style.setProperty("--shadowColor", t.shadowColor || "rgba(0,0,0,.45)");

    root.style.setProperty("--displayFontColor", t.displayFontColor);
    root.style.setProperty("--displayMutedColor", t.displayMutedColor);
    root.dataset.themePreset = name;
    return name;
  }

  function reapplyDisplayStylingAfterTheme(){
    try{
      if(typeof window.reapplyDisplayColorsFromState === "function") window.reapplyDisplayColorsFromState();
    }catch(e){}
  }

  function applyCurrentTheme(){
    const applied = applyThemePreset(getActiveThemeName());
    reapplyDisplayStylingAfterTheme();
    return applied;
  }

  function getSavedThemeDefaultPreset(){
    const settings = getThemeSettings();
    const defp = settings.themeDefaultPreset;
    return isThemePresetName(defp) ? defp : THEME_DEFAULT_PRESET;
  }

  function getSavedThemePreset(){
    // The active theme is window/tab-specific (see getActiveThemeName).
    return getActiveThemeName();
  }

  function saveThemeDefaultPreset(preset){
    const p = isThemePresetName(preset) ? preset : THEME_DEFAULT_PRESET;
    const theme = THEME_PRESETS[p] || THEME_PRESETS[THEME_DEFAULT_PRESET];
    const settings = getThemeSettings();
    settings.themeDefaultPreset = p;
    // Keep the persisted/remote "active" pinned to the default so it never
    // carries a single window's choice across tabs or devices.
    settings.themePreset = p;
    settings.bgColor = theme.bg;
    themePrefsState = normalizeThemePrefs(settings);
    persistThemePrefs();
  }

  function populateThemeSelect(sel){
    if(!sel) return;
    sel.innerHTML = getThemePresetNames().map((name)=>{
      return '<option value="' + name + '">' + THEME_PRESETS[name].label + '</option>';
    }).join("");
  }

  function renderThemePresetGrid(currentName, defaultName){
    const grid = document.getElementById("themePresetGrid");
    if(!grid) return;
    grid.innerHTML = getThemePresetNames().map((name)=>{
      const theme = THEME_PRESETS[name];
      let badges = "";
      if(name === currentName) badges += '<span class="themePresetBadge">Current</span>';
      if(name === defaultName) badges += '<span class="themePresetBadge">Default</span>';
      return '<button class="themePresetBtn' + (name === currentName ? ' active' : '') + '" data-theme="' + name + '" type="button"'
        + ' style="--themePreviewBg:' + theme.previewBg + '; --themePreviewPanel:' + theme.previewPanel + '; --themePreviewText:' + theme.previewText + '; --themePreviewMuted:' + theme.previewMuted + '; --themePreviewAccent:' + theme.previewAccent + ';">'
          + '<span class="themePresetSwatch">'
            + '<span class="themePreviewPanel"></span>'
            + '<span class="themePreviewRows">'
              + '<span class="themePreviewLines">'
                + '<span class="themePreviewLine themePreviewLineStrong"></span>'
                + '<span class="themePreviewLine themePreviewLineSoft"></span>'
              + '</span>'
              + '<span class="themePreviewChip"></span>'
            + '</span>'
          + '</span>'
          + '<span class="themePresetText">'
            + '<span class="themePresetHeader">'
              + '<span class="themePresetName">' + theme.label + '</span>'
              + badges
            + '</span>'
            + '<span class="themePresetDesc">' + theme.description + '</span>'
          + '</span>'
        + '</button>';
    }).join("");
  }

  function syncThemeUI(currentName, defaultName){
    const sel = document.getElementById("themePreset");
    const defSel = document.getElementById("themeDefaultPreset");
    const help = document.getElementById("themePresetHelp");
    const current = isThemePresetName(currentName) ? currentName : THEME_DEFAULT_PRESET;
    const defp = isThemePresetName(defaultName) ? defaultName : THEME_DEFAULT_PRESET;
    if(sel) sel.value = current;
    if(defSel) defSel.value = defp;
    renderThemePresetGrid(current, defp);
    if(help && THEME_PRESETS[current]){
      const windowNote = current === defp
        ? " Applied to this window; it's also your default for new windows."
        : " Applied to this window only — your default stays " + (THEME_PRESETS[defp] ? THEME_PRESETS[defp].label : defp) + ".";
      help.textContent = THEME_PRESETS[current].description + windowNote;
    }
  }


  function initThemeUI(){
    const sel = document.getElementById("themePreset");
    const defSel = document.getElementById("themeDefaultPreset");
    const resetBtn = document.getElementById("resetThemeBtn");
    const presetGrid = document.getElementById("themePresetGrid");
    if(!sel) return;

    populateThemeSelect(sel);
    populateThemeSelect(defSel);

    // Initialize selects from saved settings
    const savedDefault = getSavedThemeDefaultPreset();
    const current = getSavedThemePreset();
    applyCurrentTheme();
    syncThemeUI(current, savedDefault);

    // Picking a theme here (dropdown or preset grid) only restyles THIS
    // window/tab — it never touches the saved default.
    function applyWindowTheme(p){
      if(!isThemePresetName(p)) return;
      setWindowThemePreset(p);
      applyThemePreset(p);
      reapplyDisplayStylingAfterTheme();
      sel.value = p;
      syncThemeUI(p, defSel ? defSel.value : getSavedThemeDefaultPreset());
      if(typeof toast === "function") toast("Theme applied to this window.");
    }

    sel.addEventListener("change", ()=>{ applyWindowTheme(sel.value); });

    if(presetGrid){
      presetGrid.addEventListener("click", (e)=>{
        const btn = e.target && e.target.closest ? e.target.closest(".themePresetBtn") : null;
        if(!btn) return;
        applyWindowTheme(btn.getAttribute("data-theme"));
      });
    }

    // The default is the only persisted theme, and the dropdown is the only
    // way to change it. If this window hasn't picked its own theme, it's
    // showing the default, so update the live view too.
    if(defSel){
      defSel.addEventListener("change", ()=>{
        const p = defSel.value;
        saveThemeDefaultPreset(p);
        const hadOverride = !!readWindowThemeOverride(activeThemePrefsScope);
        if(!hadOverride){
          applyThemePreset(p);
          reapplyDisplayStylingAfterTheme();
          sel.value = p;
        }
        syncThemeUI(getActiveThemeName(), p);
        if(typeof toast === "function") toast("Default theme saved.");
      });
    }

    if(resetBtn){
      resetBtn.addEventListener("click", ()=>{
        // Drop this window's override so it follows the saved default again.
        clearWindowThemePreset();
        const d = getSavedThemeDefaultPreset();
        applyThemePreset(d);
        reapplyDisplayStylingAfterTheme();
        sel.value = d;
        syncThemeUI(d, d);
        if(typeof toast === "function") toast("This window reset to the default theme.");
      });
    }
  }
  window.refreshThemePrefsForSession = function(sessionLike){
    const settings = ensureThemePrefsLoaded(getThemeScopeFromSession(sessionLike));
    applyCurrentTheme();
    reapplyDisplayStylingAfterTheme();
    syncThemeUI(getActiveThemeName(), settings.themeDefaultPreset);
    return settings;
  };
  window.applyCurrentTheme = applyCurrentTheme;
  try{
    ensureThemePrefsLoaded("guest");
    applyCurrentTheme();
  }catch(e){}
// Initialize theme after settings load
  window.addEventListener("DOMContentLoaded", ()=>{
    try{ initThemeUI(); }catch(e){}
  });
